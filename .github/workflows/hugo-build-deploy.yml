name: hugo build to gh-pages

# # Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
# permissions:
#   contents: read
#   pages: write
#   id-token: write

# # Allow one concurrent deployment
# concurrency:
#   group: "pages"
#   cancel-in-progress: true

# # Default to bash
# defaults:
#   run:
#     shell: bash


on:
  push:
    branches:
      - main  # Set a branch to deploy
  pull_request:

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-22.04
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true  # Fetch Hugo themes (true OR recursive)
          fetch-depth: 0    # Fetch all history for .GitInfo and .Lastmod

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: 'latest'
          extended: true

      - name: Build
        run: hugo --minify

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        if: ${{ github.ref == 'refs/heads/main' }}
        with:
          github_token: ${{ secrets.HUGO_BUILD }}
          publish_dir: ./docs
          cname: kiumars.tk
      
      - name: Copy To Branches Action
        uses: planetoftheweb/copy-to-branches@v1.2
        env:
          key: main
          branches: gh-pages
          files: favicon.png
          
      # - name: CopyFavicon
      #   uses: actions/checkout@v2
      #   env:
      #     SRC_FOLDER_PATH: 'main'
      #     TARGET_BRANCH: 'gh-pages'
      #   run: git config --global user.name 'GitHub Action' &&\
      #       git config --global user.email 'action@github.com' && git fetch &&\
      #       git checkout $TARGET_BRANCH &&\
      #       git checkout ${GITHUB_REF##*/} -- favicon.png &&\
      #       git add -A &&\
      #       git diff-index --quiet HEAD ||  git commit -am "deploy favicon" &&\
      #       git push origin $TARGET_BRANCH